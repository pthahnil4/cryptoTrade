# 数据存储说明

> 适用项目：cryptoTrade（加密货币交易监控台）
> 更新日期：2026-08-21

---

## 一、总体设计：不用数据库，全部文件存储

本项目的**所有数据都不依赖数据库**（无 MySQL / SQLite / Redis），全部以**普通文件**的形式保存在磁盘上。原因：

- 数据量小（几十 KB ~ 几 MB），文件读写性能完全够用
- 文件可以直接用文本编辑器查看、手动修改、备份拷贝，零运维成本
- 服务器上无需安装和维护数据库服务

数据分两大类：

| 类别 | 特点 | 位置 |
|---|---|---|
| **A. 代码** | 随项目更新走，可被覆盖 | 整个项目文件夹 |
| **B. 数据/运行状态** | 程序运行产生，不能被覆盖 | 外置数据目录（见第五节），日志/CSV 仍在项目内 |
| **C. 密钥/配置** | 每台机器不同，手动维护 | `crypto/` 项目内（如 `api_config.py`） |

所有 JSON 数据与运行状态文件均已外置（见第五节），CSV 行情与日志仍保存在项目内。

---

## 二、四种文件存储格式

| 格式 | 扩展名 | 用途 | 特点 |
|---|---|---|---|
| **JSON** | `.json` | 结构化数据、运行状态、配置 | 树状嵌套、可读性强，程序整体读/整体写 |
| **CSV** | `.csv` | 表格数据（币种清单、行情、回测结果） | 每行一条记录，可用 Excel 打开 |
| **JSONL** | `.jsonl` | 追加式流水（成交记录） | 一行一个 JSON 对象，只追加不修改，天然防损坏 |
| **文本日志** | `.log` | 运行日志 | 按大小滚动（10MB × 10 份），超限自动轮转 |

---

## 三、数据文件总览（按模块）

### 1. 任务计划模块（已迁移 MySQL）⭐

| 存储 | 格式 | 内容 |
|---|---|---|
| MySQL `task_plans` 等表 | 关系表 | 全部任务计划：计划（学习/交易）、每轮任务卡（100格打卡记录、子目标、过程小记、复盘、结算记录） |

- 原 `task_plans.json` 已完成迁移入库（见 `migrate_json_to_db.py`），后端接口契约不变
- 结构是嵌套树（计划 → 卡片 → 100 格 → 打卡记录），入库后按关系表拆分存储

### 2. 币种与行情模块

| 文件 | 格式 | 内容 | 能否重建 |
|---|---|---|---|
| `crypto_coins.csv` | CSV | 币种清单 + 多周期趋势分析结果（1H/4H/1D 的趋势、MACD、ADX、SAR 等指标） | 重新扫描可重建，但历史盈亏数据会丢 |
| `star币种行情.csv` | CSV | 星标币种实时行情与策略信号（现价、方向、预测、建议操作） | 可重建 |
| `market_scan_cache.json` | JSON | 市场扫描榜单缓存（涨幅榜/跌幅榜/成交额榜/趋势榜，含 24h 涨跌、效率系数等），**已外置** | 可重建（缓存而已） |

> 旧版曾有 `batch_progress.json`（批量扫描进度），现已改为内存维护，该文件已废弃删除。

### 3. 热量管理模块（已外置）

| 文件 | 格式 | 内容 |
|---|---|---|
| `calorie_records.json` | JSON | 每日热量记录（摄入/消耗/缺口） |
| `calorie_food_db.json` | JSON | 食物热量数据库（约 900 行，用户自定义食物库） |

### 3.5 随笔复盘模块（已外置）

| 文件 | 格式 | 内容 |
|---|---|---|
| `journal_notes.json` | JSON | 随笔复盘笔记（长期主义记录），路径优先级由环境变量 `CRYPTO_JOURNAL_DATA_DIR` 控制，其余规则同第五节 |

### 4. 监控台配置

| 文件 | 格式 | 内容 |
|---|---|---|
| `config.json` | JSON | 监控台配置：全部币种列表 `all_coins`、默认选中 `default_selected`、星标币种 `starred_coins`、浮动币种 `floating_coins` |

> 注意：这个文件会被前端"勾选星标/添加浮动币种"操作自动修改，属于**数据**，不是纯配置。

### 5. 定时任务运行状态（已外置）

| 文件 | 格式 | 内容 |
|---|---|---|
| `scheduler_state.json` | JSON | 各币种长短周期方向记录（重启后恢复，防漏判方向反转） |
| `reverse_guard_state.json` | JSON | 反向持仓风控计时器（预警 → 强平倒计时，重启不重置） |
| `manual_pause_state.json` | JSON | 人工强平冷却期（防止重启后立刻自动开回仓） |
| `tp_runtime_state.json` | JSON | 止盈引擎运行时状态（峰值价、已触发档位） |
| `position_order_state.json` | JSON | 双仓位本地账本（held/均价/slots/委托算法），每次下单立即落盘 |
| `instrument_spec_cache.json` | JSON | 合约规格缓存（ctVal/lotSz/minSz，24h TTL，可重建） |

> 以上均为**崩溃/重启恢复**用途，删了也能跑，但会丢失恢复上下文（可能造成漏平仓/重复开仓，不建议乱删）。现已统一存放在外置数据目录，不随代码更新被覆盖。

### 6. 日志系统

| 文件 | 格式 | 内容 |
|---|---|---|
| `logs/task_scheduler.log` | 文本日志 | 定时任务执行流（每轮开始/结束、分析、决策、异常） |
| `logs/trade_operations.log` | 文本日志 | 实盘交易操作流（开仓/平仓/订单详情、API 响应） |
| `logs/trade_journal.jsonl` | JSONL | 机器可读的成交流水（每笔实际成交一行，供"实盘 vs 策略理论"对比） |
| `strategy.log` | 文本日志 | 策略适配层日志 |

- 日志滚动策略：单个文件上限 **10MB**，最多保留 **10 份**历史（`.1` ~ `.10`），自动轮转，不会无限膨胀
- 每条日志带 `run_id`（如 `R20260708-143000`），可与交易操作日志交叉定位

### 7. 回测模块

| 文件 | 格式 | 内容 |
|---|---|---|
| `backtest/data_cache/*.csv` | CSV | 历史 K 线缓存（按币种×周期分页），网络不可用时复用 |
| `backtest/results/*.csv` | CSV | 回测结果：交易记录 `trades`、成交明细 `fills`、权益曲线 `equity` |
| `backtest/results/*.log` | 文本日志 | 回测操作明细 |

---

## 四、密钥/配置类文件（C 类，不随数据备份走）

这些文件**每台机器不一样**，部署时手动配置，更新代码时不要覆盖：

| 文件 | 内容 |
|---|---|
| `crypto/api_config.py` | OKX 交易所 API 密钥（多账号） |
| `MACD_Smoothed_ADX_Pro3_Project/api_config.py` | 策略模块 API 密钥 |
| `task/config/config_trend_range.json` | 趋势/区间交易策略参数 |
| `task/config/email_config.py` | 邮件通知账号 |
| `demo/influxdb_config.py` | InfluxDB 连接配置（仅 demo 演示用） |
| `demo/ws_config.py` | WebSocket 配置（仅 demo 演示用） |
| `data/web_token.txt` | Web 面板访问口令（单行，见 `crypto/web_auth.py`） |

**这些文件含明文凭据，一律不入库**（写进了 `.gitignore`），每个都配了一份
`*.example.py` 脱敏模板，新机器按模板拷一份再填真实值。例外是
`data/web_token.txt`：它不在仓库里，需每台机器自己创建（或改用环境变量
`CRYPTO_WEB_TOKEN`）。

---

## 五、数据存储机制（重点）

**所有业务数据均已迁移 MySQL**（分 8 批完成，迁移脚本 `migrate_json_to_db.py`、建表留档 `db_schema.sql`）。数据权威源为数据库，更新代码覆盖项目文件夹时数据不受影响。

**项目内 `data/` 目录**仅存放兜底文件：

- `db_url.txt`：数据库连接串（单行，无环境变量时的兜底）
- `web_token.txt`：Web 面板访问口令（单行，无环境变量时的兜底；没有此文件则远程一律 403）
- `instrument_spec_cache.json` / `market_scan_cache.json`：缓存双写文件（DB 不可用时的兜底数据源）

**数据库连接串优先级**（`crypto/database.py`）：

```
1. 环境变量 CRYPTO_DB_URL（最高优先）
2. 数据目录下的 db_url.txt（单行连接串）
```

**Web 访问口令优先级**（`crypto/web_auth.py`，与上面同一套外置目录约定）：

```
1. 环境变量 CRYPTO_WEB_TOKEN（最高优先）
2. 数据目录下的 web_token.txt（单行口令）
3. 都没有 → 只放行本机，远程一律 403
```

**数据目录优先级**（`crypto/data_paths.py`）：

```
1. 环境变量 CRYPTO_PLAN_DATA_DIR（显式指定，最高优先）
2. 项目内 data/ 目录（默认，随项目一起部署）
```

**宝塔部署**：把连接串写入项目内 `data/db_url.txt`（或配置环境变量 `CRYPTO_DB_URL`）即可，无需其他配置。
想从手机/其它设备访问面板，还要多一步：把访问口令写入 `data/web_token.txt`（或设环境变量
`CRYPTO_WEB_TOKEN`），否则远程打不开——详见 `项目说明.txt` 的部署章节。

重启服务后生效。

---

## 六、备份与恢复建议

| 优先级 | 对象 | 说明 |
|---|---|---|
| ⭐ 必须备份 | MySQL `crypto` 库（mysqldump 整库导出） | 所有业务数据权威源，不可重建 |
| ⭐ 必须备份 | `data/db_url.txt` | 数据库连接串，丢失后需重新配置 |
| ⭐ 必须备份 | `data/web_token.txt` | Web 访问口令，不入库；丢了等于自己把自己关在门外 |
| 建议备份 | `crypto/config.json`、`crypto/task/config/config_trend_range.json` | 配置双写兜底文件，DB 中已有副本 |
| 可丢弃 | `data/` 下两个缓存 JSON | 缓存双写文件，自动重建 |
| 可丢弃 | `logs/`、`strategy.log` | 日志，删除无影响 |
| 可丢弃 | `backtest/data_cache/`、`backtest/results/` | 回测产物，可重新生成 |

**迁移服务器时**：部署代码 + 导入 MySQL 备份即可；`data/db_url.txt` 随项目一起上传（或改用环境变量 `CRYPTO_DB_URL`）。
