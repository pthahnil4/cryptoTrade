# 随笔记录与复盘功能设计文档

> 版本：v1.1 设计方案（已按评审意见确认修订）
> 编写日期：2026-08-10
> 定位：个人所思所想、心得、经验、交易复盘的轻量级记录与沉淀系统
>
> **评审确认项（2026-08-10）**：① 首版包含导出功能；② 首版包含统计看板；③ 预置 6 个标签，允许个人增改；④ 页面入口置于导航"任务计划"之后。

---

## 一、功能定位与目标

### 1.1 一句话定位

一个**低门槛、按条记录、带标签、可检索、可复盘**的个人思考与交易复盘笔记本，作为现有量化交易系统的"思考侧"补充——行情和代码是数据，但**决策背后的想法**目前无处沉淀。

### 1.2 解决什么问题

| 痛点 | 对应能力 |
| --- | --- |
| 盘中/盘后一闪而过的想法没记下来，过后全忘 | 随笔速记：一条文本即存，10 秒完成 |
| 复盘散落在聊天记录、脑子里，无法回看 | 复盘记录：结构化条目 + 时间线 |
| 记录多了翻不动，找不到当时那条经验 | 标签分类 + 关键词搜索 + 时间筛选 |
| 同一类错误反复犯，经验没有沉淀成结论 | 经验提炼：把复盘条目"提炼"为永久经验卡 |
| 不知道哪些想法后来被验证了 | 条目可"关联"（前因→后果），支持补记验证结果 |

### 1.3 设计原则

1. **记录零摩擦**：随手记不需要选分类、不需要标题，正文+标签即可保存。
2. **复盘有结构**：复盘类条目提供引导式字段（事件/判断/结果/教训），但全部可选。
3. **数据诚实、情绪友好**：复盘亏损不指责，只沉淀教训；与任务计划模块的长期主义理念一致。
4. **与项目现有架构完全一致**：Flask 蓝图 + JSON 外置数据文件 + Jinja 模板 + 原生 JS，不引入新依赖。

---

## 二、总体架构

```
┌─────────────────────────────────────────────────┐
│  前端：templates/journal.html + static/js/journal.js │
│  （时间线列表 / 快速记录框 / 标签栏 / 复盘弹窗 / 统计看板） │
└──────────────────────┬──────────────────────────┘
                       │ fetch /journal/api/*
┌──────────────────────▼──────────────────────────┐
│  后端：crypto/journal_routes.py                    │
│  Blueprint: journal_bp                            │
│  路由：/journal（页面） + /journal/api/*（CRUD/统计）  │
└──────────────────────┬──────────────────────────┘
                       │ MySQL 读写（SQLAlchemy）
┌──────────────────────▼──────────────────────────┐
│  数据：MySQL journal_notes / journal_tags / note_tags 表  │
│  （已迁移入库，原 journal_notes.json 仅作历史留档）      │
└─────────────────────────────────────────────────┘
```

**新增文件清单**：

| 文件 | 说明 |
| --- | --- |
| `crypto/journal_routes.py` | 蓝图 + 数据操作层 + API |
| `crypto/templates/journal.html` | 页面模板（复用 `nav.html` 导航） |
| `crypto/static/js/journal.js` | 前端交互逻辑 |
| `crypto/static/css/style.css` | 追加随笔页样式（不新建 css 文件） |
| `crypto/app.py` | 注册 `journal_bp`（一行 import + 一行 register） |

---

## 三、数据模型

### 3.1 存储文件 `journal_notes.json`

```json
{
  "initialized": true,
  "tags": [
    { "name": "交易心得", "color": "#4a90d9" },
    { "name": "策略思考", "color": "#7e57c2" },
    { "name": "复盘",     "color": "#e67e22" },
    { "name": "教训",     "color": "#e74c3c" },
    { "name": "经验",     "color": "#27ae60" },
    { "name": "生活随笔", "color": "#95a5a6" }
  ],
  "notes": [
    {
      "id": "note_a1b2c3d4",
      "type": "note",
      "content": "BOLL 收口后连续三根小阳，可能要变盘，先别追多。",
      "tags": ["交易心得"],
      "pinned": false,
      "distilled": false,
      "linked_from": "",
      "created_at": "2026-08-10 14:32:05",
      "updated_at": "2026-08-10 14:32:05"
    },
    {
      "id": "note_e5f6g7h8",
      "type": "review",
      "content": "追高做多被套，止损晚了 2 个点。",
      "tags": ["复盘", "教训"],
      "review": {
        "subject": "BTC-USDT-SWAP 多单",
        "decision": "看到放量突破前高，市价追多 5 倍杠杆",
        "outcome": "假突破回落，-3.2% 止损离场",
        "lesson": "突破不追单，等回踩确认再进场；止损必须预设好"
      },
      "pinned": false,
      "distilled": true,
      "linked_from": "",
      "created_at": "2026-08-09 22:10:41",
      "updated_at": "2026-08-10 09:00:00"
    }
  ]
}
```

### 3.2 条目（note）字段说明

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `id` | str | 是 | `note_` + uuid8 位，与 plan 模块 `_new_id()` 同款 |
| `type` | str | 是 | `note`（随笔）/ `review`（复盘），默认 `note` |
| `content` | str | 是 | 正文（核心字段，随笔只要这一个就够） |
| `tags` | list | 否 | 标签名数组，必须来自 `tags` 字典（允许先建标签再引用） |
| `review` | object | 否 | 复盘引导字段，仅 `type=review` 时使用，见 3.3 |
| `pinned` | bool | 否 | 置顶（用于"重要结论"钉在时间线顶部） |
| `distilled` | bool | 否 | 是否已提炼为经验（复盘条目专用） |
| `linked_from` | str | 否 | 关联来源：① 条目间关联＝原条目 id（补记验证结果时指向原条目，形成前因→后果链）；② 跨模块关联＝`{模块}:{引用}` 格式，目前支持 `calorie:YYYY-MM-DD`（关联热量记录某日，热量页入口深链 `/journal?link=calorie:日期` 预填，条目卡片可点击跳回热量页）；后续可扩展交易复盘等模块前缀 |
| `created_at` | str | 是 | `YYYY-MM-DD HH:MM:SS`，与项目统一时间格式 |
| `updated_at` | str | 是 | 同上，编辑时刷新 |

### 3.3 复盘引导字段（`review` 对象，全部可选）

| 字段 | 引导语 | 说明 |
| --- | --- | --- |
| `subject` | 复盘对象 | 哪笔交易/哪个策略/哪件事（如 `BTC-USDT-SWAP 多单`） |
| `decision` | 当时怎么做的 | 决策与依据，记录"当时为什么这么想" |
| `outcome` | 实际结果 | 客观结果（盈亏/成败），数据诚实 |
| `lesson` | 下次怎么办 | 提炼出的教训/改进点，复盘的核心产出 |

> 设计理念：`content` 写自由发挥的整体叙述，`review` 四个字段是"快速填空式"结构。
> 两者互不强制——简单复盘只写 `content` 也行，深度复盘四格全填。

### 3.4 标签（tag）字段说明

| 字段 | 说明 |
| --- | --- |
| `name` | 标签名，唯一，≤8 字 |
| `color` | 展示色值，创建时从预设色板中轮换分配 |

预置 6 个标签（见 3.1 示例），**允许个人自由增改**：可新增自定义标签（自动分配颜色）、修改标签颜色、删除不用的标签。删除标签时同步从所有条目中移除该标签引用（不删条目）。

---

## 四、功能设计

### 4.1 快速记录（核心入口）

- 页面顶部固定"随手记"输入框 + 标签多选 chips + 【记录】按钮。
- 正文非空即可保存；不选标签默认归入 `随笔` 类（不打标也能存，零摩擦）。
- Ctrl+Enter 快捷提交。
- 提供「复盘模式」开关：打开后输入区展开四格引导（对象/决策/结果/教训），保存为 `type=review`。

### 4.2 时间线列表

- 按 `created_at` 倒序，**按日期分组**显示（"今天 / 昨天 / 2026-08-08"）。
- 每条显示：时间（HH:MM）、正文、标签 chips、类型徽标（随笔/复盘）、置顶图标。
- 复盘条目卡片额外展示四格摘要（有值才显示）。
- 有关联条目（`linked_from`）时显示"↳ 后续验证"链接，点击跳转定位。
- 置顶条目固定在当前列表最上方，带 📌 标记。

### 4.3 筛选与搜索

| 维度 | 交互 |
| --- | --- |
| 标签 | 左侧/顶部标签栏点击筛选，可多选（取并集）；每个标签显示条目数 |
| 类型 | 全部 / 随笔 / 复盘 三个 tab |
| 时间 | 下拉快捷：近 7 天 / 近 30 天 / 本月 / 全部 |
| 关键词 | 搜索框全文匹配 `content` + `review` 四格内容 |

以上条件可叠加，前端本地过滤（数据量在 JSON 单文件承受范围内，见第七节）。

### 4.4 条目操作

| 操作 | 说明 |
| --- | --- |
| 编辑 | 弹窗编辑正文/标签/复盘四格，保存刷新 `updated_at` |
| 删除 | MDialog 二次确认（项目统一弹窗组件） |
| 置顶/取消置顶 | 重要结论钉在顶部 |
| 补记关联 | 选择一条历史条目作为"前因"，当前新条目记录"后续验证"（设置 `linked_from`） |
| 提炼为经验 | 复盘条目专属：把 `lesson` 标记 `distilled=true`，进入经验库视图 |

### 4.5 经验库（复盘的沉淀产出）

- 独立视图（页面内 tab 切换）：只展示 `distilled=true` 条目的 `lesson`。
- 按标签分组展示成"经验卡片墙"，作为每次开新仓前的 checklist。
- 体现"复盘 → 教训 → 经验 → 下次决策"的闭环。

### 4.6 统计看板（首版包含）

- 页面内独立 tab「统计」，首版交付：
- 总条目数、复盘数、经验数、本月记录数、连续记录天数（与任务计划模块"在场天数"理念呼应：今天记了就算在场）。
- 标签分布小图（复用项目内已有的 `echarts.min.js`，不新增依赖）。
- 近 15 周记录日历（GitHub 贡献图风格，与任务计划页在场日历同款样式）。

### 4.7 导出功能（首版包含）

- 入口：时间线视图工具栏的【导出】按钮，导出**当前筛选条件下**的条目（不筛选即导出全部）。
- 两种格式，弹窗二选一：
  | 格式 | 内容 | 用途 |
  | --- | --- | --- |
  | Markdown | 按日期分组，每条含时间、正文、标签、复盘四格 | 阅读/归档/粘贴到笔记 |
  | JSON | 原始数据结构（含全部字段） | 备份/迁移/后续二次处理 |
- 实现方式：后端 `GET /journal/api/export?format=md|json` 生成文件流（`Content-Disposition: attachment`），文件名形如 `journal_export_2026-08-10.md`；浏览器直接下载，前端无需处理文件内容。
- 编码统一 UTF-8；Markdown 导出中正文保持原文，不做转义破坏。

---

## 五、API 设计

统一返回格式与项目现有接口一致：`{"code": 200, "message": "success", "data": ...}`。

| 方法 | 路径 | 说明 | 入参 |
| --- | --- | --- | --- |
| GET | `/journal` | 页面路由，render_template | — |
| GET | `/journal/api/list` | 条目列表 + 标签字典 + 统计摘要 | `tag`、`type`、`keyword`、`days`、`page`、`page_size`（可选，服务端也做一遍过滤兜底） |
| POST | `/journal/api/add` | 新增条目 | `content`(必填)、`type`、`tags[]`、`review{}`、`linked_from` |
| POST | `/journal/api/update` | 编辑条目 | `id`、`fields{content/tags/review/pinned}` |
| POST | `/journal/api/delete` | 删除条目 | `id` |
| POST | `/journal/api/pin` | 置顶切换 | `id` |
| POST | `/journal/api/distill` | 提炼经验切换 | `id` |
| GET | `/journal/api/tags` | 标签列表（含每个标签条目数） | — |
| POST | `/journal/api/tag-save` | 新增/改色标签 | `name`、`color` |
| POST | `/journal/api/tag-delete` | 删除标签（同步清理条目引用） | `name` |
| GET | `/journal/api/stats` | 统计看板数据 | — |
| GET | `/journal/api/export` | 导出条目（文件流下载） | `format`（`md`/`json`）、`tag`、`type`、`keyword`、`days`（与 list 同参，导出筛选后的结果） |

**校验规则**：
- `content` 非空（strip 后），否则 400。
- `tags` 中未注册的新标签名：自动创建（自动分配颜色）后使用——保持记录零摩擦。
- 时间字段统一走 `_normalize_datetime_str()` 同款逻辑，存储 `YYYY-MM-DD HH:MM:SS`。

---

## 六、前端页面设计（增强版 UI）

> 设计基调：**轻盈、温暖、有呼吸感**。延续项目现有浅色主题（`--bg-color: #f4f7f6` + 白卡片），
> 但随笔页作为"思考空间"，在细节上更柔和：大量圆角、淡渐变、微动效，
> 让"记录"本身成为一种愉悦的仪式感，而不是填表。

### 6.1 视觉语言

**色彩体系**（复用 `style.css` CSS 变量，新增 `.journal-*` 命名空间局部变量）：

| 元素 | 处理 |
| --- | --- |
| 页面背景 | 在 `--bg-color` 基础上叠加极淡的顶部渐变（如 `linear-gradient(180deg, #eef4f8 0%, #f4f7f6 240px)`），避免大面积纯灰的沉闷感 |
| 卡片 | 白底 + `border-radius: 14px` + 柔和阴影 `0 2px 12px rgba(0,0,0,0.06)`；悬停时阴影加深并轻微上浮（`translateY(-2px)`，200ms 过渡） |
| 主按钮 | 蓝→青渐变（`linear-gradient(135deg, #4a90d9, #5cb85c)`，项目已有同款渐变），悬停亮度 +5% |
| 类型徽标 | 随笔＝淡蓝胶囊，复盘＝淡橙胶囊；复盘卡片左边框用 3px 橙色渐变竖条与随笔区分 |
| 置顶卡片 | 淡黄渐变底（`#fffdf5 → #fff8e1`，与项目现有渐变语汇一致）+ 📌 角标 |
| 标签 chips | 圆角胶囊（radius 20px），淡色底 + 同色系文字；选中时填充为标签主色 + 白字，带 0.2s 弹性过渡 |

**字体与排版**：

- 正文 15px / 行高 1.7，卡片内正文支持多行换行原样呈现（`white-space: pre-wrap`），尊重随笔的随性排版。
- 日期分组标题用 13px 灰字 + 两侧细分隔线；"今天/昨天"用相对称呼，更有温度。
- 时间戳用等宽字色 `--text-muted`，不抢正文视线。

### 6.2 布局（自上而下）

```
┌──────────────────────────────────────────────┐
│  导航（复用 nav.html，"任务计划"之后追加"随笔复盘"入口）│
├──────────────────────────────────────────────┤
│  页面标题区：✍️ 随笔与复盘 + 副标题 + [时间线][经验库][统计]  │
├──────────────────────────────────────────────┤
│  数据概览卡：总记录 N · 复盘 N · 经验 N · 连续 N 天🔥   │
├──────────────────────────────────────────────┤
│  随手记卡片（焦点时边框泛光）：                         │
│    ✍️ 输入框（自动增高） + [复盘四格开关]              │
│    标签chips多选 + [Ctrl+Enter 快捷提示]  [记录]      │
├──────────────────────────────────────────────┤
│  工具栏：[类型tab] [时间筛选] [搜索框🔍] [导出⬇]      │
├─────────────┬────────────────────────────────┤
│  标签栏（粘性） │  时间线列表（按日分组）               │
│  全部(128)   │  📌 置顶卡片（淡黄底）                  │
│  交易心得(42) │  ── 今天 ──                          │
│  复盘(31)    │  [卡片] 14:32 正文… #交易心得           │
│  教训(12)    │  ── 昨天 ──                          │
│  …          │  [复盘卡片] 橙色竖条 + 四格摘要…         │
│  [+新标签]    │  …                                  │
└─────────────┴────────────────────────────────┘
```

**布局细节**：

- 整体居中容器 `max-width: 1100px`；左侧标签栏宽 180px、**sticky 定位**，滚动时始终可见。
- 窄屏（<900px）标签栏折叠为顶部横向滚动 chips，时间线单列。
- 卡片悬停时右上角浮现操作图标组（编辑✏️/置顶📌/删除🗑/关联🔗），不悬停时不干扰阅读。

### 6.3 核心交互体验

**随手记（写的那一刻最重要）**：

- 输入框 focus 时卡片边框泛淡蓝光晕（`box-shadow: 0 0 0 3px rgba(74,144,217,0.15)`），提示"我在听"。
- 输入框随内容自动增高（最多 8 行后出滚动条）。
- 保存成功：新卡片从顶部**滑落渐显**（300ms ease-out）+ toast "已记录 ✨"，输入框清空并保留焦点——方便连续速记。
- Ctrl+Enter 提交；输入区右下角灰字小快捷键提示。
- 复盘模式：开关打开时四格引导区**平滑展开**（max-height 过渡），每格带浅色引导 placeholder（如"当时为什么这么想…"）。

**时间线浏览**：

- 首次加载按日期分组逐批淡入（stagger 动画，每组延迟 60ms），不是一下子砸出来。
- 关联条目点击"↳ 后续验证"平滑滚动定位目标卡片，目标卡片短暂高亮闪烁（黄色淡光 1s）。
- 删除前 MDialog 确认；删除时卡片向右滑出淡出，列表自然合拢。

**空态设计（第一印象很关键）**：

- 无任何记录时：中央插画风占位（纯 CSS/emoji 组合，不引入图片资源）+ 文案"还没有记录，写下此刻的想法吧 ✍️"+ 一键聚焦输入框按钮。
- 筛选无结果时："没有匹配的记录，换个筛选条件试试"+ 一键清空筛选按钮。

**加载与反馈**：

- 所有请求期间按钮进入 loading 态（转圈 + 禁用），防止重复提交。
- 页面首次加载用骨架屏（3 个灰色卡片闪烁占位），而非白屏。
- 成功/失败统一用项目已有的 MDialog toast 风格反馈。

### 6.4 三个视图的专属设计

**时间线**：见 6.2/6.3；复盘卡片的四格摘要用小字标签引导（对象/决策/结果/教训），四格中只渲染有值的格子。

**经验库**：

- 卡片墙布局（响应式 grid），每张经验卡顶部是标签色竖条 + 大字号 `lesson` 文本，下方灰色小字链回原复盘条目日期。
- 卡片入场淡入 stagger；悬停显示"查看原复盘"链接。
- 空态："还没有提炼过经验——去复盘一条记录，把教训变成财富 💡"。

**统计看板**：

- 顶部四张指标小卡（带图标 + 数字大字 + 淡色渐变底，与项目现有指标卡风格对齐）。
- 中部两栏：标签分布环形图（echarts，用标签自身颜色渲染） + 近 15 周记录日历（与任务计划页在场日历同款格子样式，悬停 tooltip 显示当日条数）。
- 连续记录天数旁展示 🔥，历史最佳小字标注，呼应长期主义理念。

### 6.5 技术约定

- 页面内顶部 tab：`时间线` / `经验库` / `统计`（统计看板为首版范围，独立 tab 呈现）；tab 切换用下划线滑动指示条（transform 过渡）。
- 弹窗统一使用项目已有的 `mdialog.js`（MDialog 组件），不新造弹窗。
- 动画统一时长规范：微交互 150~200ms，入场/退场 250~300ms，缓动统一 `ease-out`；遵循"动效服务于反馈，不做纯装饰"原则。
- 样式追加在 `style.css` 末尾，全部使用 `.journal-` 前缀命名空间，不污染全局。
- 移动端自适应（宝塔部署后手机也能随时记）：<600px 时卡片操作图标改为常驻小按钮，输入框高度适配触屏。
- 所有图标用 emoji/Unicode 符号（项目现有风格），不引入图标字体依赖。

---

## 七、数据量与性能考量

- 单文件 JSON 方案与 plan/calorie 模块一致；按**每条 500 字**估算，1 万条 ≈ 10MB，读写仍无压力。预期个人使用量级（每天 1~5 条，一年 ≤2000 条）远未触及瓶颈。
- 若未来超过 1 万条，预留升级路径：按年分文件（`journal_notes_2026.json`），`_load_notes()` 合并读取、写入只落当年文件——接口层无需改动。
- 读写均显式 `encoding='utf-8'`（项目编码规范）。

---

## 八、与现有模块的关系

| 关联点 | 说明 |
| --- | --- |
| 任务计划模块（plan） | 理念一致（长期主义、在场日历风格统计）；**数据完全独立**，不混存于 task_plans.json |
| 交易模块（trend_range_trader 等） | 首版不自动联动；预留 v1.1 能力：交易邮件通知/交易日志（trade_journal.jsonl）中的开平仓记录可一键"导入为复盘草稿" |
| 导航 | `nav.html` 在**"任务计划"之后**追加入口，路由 `/journal` |

---

## 九、实施步骤与工作量

| 步骤 | 内容 | 产出 |
| --- | --- | --- |
| 1 | 数据层：路径解析、加载/保存、ID/时间工具 | `journal_routes.py` 前半 |
| 2 | API：条目 CRUD + 标签管理 + 统计 + 导出 | `journal_routes.py` 后半 |
| 3 | `app.py` 注册蓝图，`nav.html` 在"任务计划"后加入口 | 可 curl 验证接口 |
| 4 | 前端页面：时间线 + 快速记录 + 标签筛选 + 导出按钮 | `journal.html` + `journal.js` |
| 5 | 复盘四格弹窗、经验库视图、统计看板 tab、动效与空态打磨 | 前端完善 |
| 6 | 自测清单：增删改查/筛选/置顶/提炼/关联/导出(md+json)/统计/动效/移动端 | 验收 |

预计单文件后端约 500~600 行，前端合计约 1100~1300 行（UI 增强后样式与动效占比较高），与 calorie 模块体量相当。

---

## 十、首版范围（已确认）

**首版（v1.0）做**：快速记录、复盘四格、标签管理（预置 6 个 + 个人自由增改）、筛选搜索、置顶、提炼经验库、**统计看板**、**导出（Markdown/JSON）**。

**明确不做（留给后续版本）**：
- ❌ 图片/附件上传（个人文字记录够用，避免文件管理复杂度）
- ❌ 多用户/权限（单用户本地系统）
- ❌ 自动抓取行情快照附在复盘条目里（v1.1 再议）
- ❌ 导入功能（有导出即可备份；导入需求出现后再加）

**已确认决策（原待确认问题）**：
1. ✅ 统计看板纳入首版（见 4.6 节）。
2. ✅ 预置 6 个标签保持不变，允许个人增改（见 3.4 节）。
3. ✅ 页面入口置于导航"任务计划"之后（见第八节）。
