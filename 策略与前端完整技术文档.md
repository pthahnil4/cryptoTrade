# 加密货币策略监控系统 — 完整技术实现文档

> 本文档用于在另一个项目中**完全复刻**当前系统的三个策略效果与页面功能，
> 涵盖策略算法、数据处理逻辑、前后端 API 契约、UI 结构与渲染规则。
>
> 涉及源码：
> - `crypto/strategy/pro3_singletimeframe.py`（1338 行）
> - `crypto/strategy/pro3_dualtimeframe.py`（836 行）
> - `crypto/strategy/boll_limit_dualtimeframe.py`（371 行）
> - `crypto/trend_strategy_boll_limit_scheduler_like.py`（754 行，BOLL 核心）
> - `crypto/strategy_util.py`（策略分发器）
> - `crypto/real_strategy_adapter.py`（主页卡片数据 + 币种配置管理）
> - `crypto/app.py`（Flask 路由）
> - `crypto/templates/index.html` + `crypto/static/js/main.js`（主页）
> - `crypto/templates/detail.html`（详情页，内联 JS）

---

## 目录

0. [系统总体架构](#0-系统总体架构)
1. [三个策略的功能、实现逻辑与区别](#1-三个策略的功能实现逻辑与区别)
2. [周期选择器（短周期与长周期）](#2-周期选择器)
3. [币种选择器](#3-币种选择器)
4. [策略选择按钮的切换逻辑](#4-策略选择按钮的切换逻辑)
5. [数据展示面板的渲染机制](#5-数据展示面板的渲染机制)
6. [统计指标的计算方法与显示内容](#6-统计指标的计算方法与显示内容)
7. [前端 JS 与后端 API 交互契约](#7-前端-js-与后端-api-交互契约)
8. [两页面完整功能流程](#8-两页面完整功能流程)
9. [复刻清单](#9-复刻清单)

---

## 0. 系统总体架构

```
浏览器
 ├─ index.html + main.js ──→ /api/strategy/config、/api/strategy/coin/<symbol>、/api/batch/*
 └─ detail.html(内联JS)  ──→ /api/strategy/detail/<symbol>?bar=&strategy=&long_bar=
        │
Flask (app.py)
 ├─ /api/strategy/coin/*   → real_strategy_adapter.calculate_single_coin_data()   （主页卡片）
 └─ /api/strategy/detail/* → strategy_util.get_strategy_detail()                  （详情页分发器）
        │
strategy_util.py（按 strategy 参数分发；single/dual 分支先 del sys.modules 强制重载模块）
 ├─ single → strategy/pro3_singletimeframe.get_strategy_full_data()
 ├─ dual   → strategy/pro3_dualtimeframe.get_strategy_full_data_dual()
 └─ boll   → strategy/boll_limit_dualtimeframe.get_boll_strategy_full_data()
                 └─ 内部调用 trend_strategy_boll_limit_scheduler_like（backtrader 核心）
        │
OKX API（标记价格K线 get_mark_price_candlesticks，约500根）
```

- 后端统一响应格式：`{code: 200/400/500, message: str, data: {...}}`
- 前端为原生 JS（无框架），弹窗用自研 `MDialog`，按钮用 `MButton`（m-btn 系列类名）
- 回测框架：**backtrader**（`bt.Cerebro` + `bt.Strategy` + `PandasData`）

---

## 1. 三个策略的功能、实现逻辑与区别

### 1.0 三策略共享的基础算法

#### (a) K线数据获取（`pro3_singletimeframe._fetch_kline_data`，三策略统一）

- OKX `MarketAPI.get_mark_price_candlesticks(instId, bar)`：首页 100 根 + `after` 参数向前翻页 4 次，共约 **500 根**；
- 每次调用**新建 MarketAPI 实例**（避免 OKX SDK 长连接失效问题）；
- httpx 显式超时：`connect=15 / read=20 / write=15 / pool=15` 秒；
- 失败重试 4 次，退避 1/2/3/3 秒封顶；翻页失败时降级返回已有数据（弱网容错）；
- 时间戳 ms → `pd.to_datetime` 后 **+8 小时**（UTC → 北京时间），升序排列；
- 输出列：`open / high / low / close / confirm`。

BOLL 策略原本用 1m CSV 重采样，适配器 `boll_limit_dualtimeframe._make_direct_period_fetcher(instId)`
通过 **monkey-patch 替换** 核心模块的 `get_period_data`，改为上述 OKX 直接取数（带周期缓存），
调用完毕在 `finally` 中恢复原函数。

#### (b) ADX 计算（Wilder 平滑，`calculate_adx(df, period=14)`）

```
prev_close = close.shift(1)
TR  = max(high-low, |high-prev_close|, |low-prev_close|)
high_diff = high - high.shift(1);  low_diff = low.shift(1) - low
+DM = high_diff if (high_diff > low_diff and high_diff > 0) else 0
-DM = low_diff  if (low_diff > high_diff and low_diff > 0)  else 0
alpha = 1/14
ATR_ADX    = TR.ewm(alpha=alpha, adjust=False).mean()
+DM_smooth = +DM.ewm(alpha=alpha, adjust=False).mean()（-DM 同理）
+DI = 100 * +DM_smooth / ATR_ADX；-DI 同理
DX  = 100 * |+DI - -DI| / (+DI + -DI)   （分母为 0 时取 0）
ADX = DX.ewm(alpha=alpha, adjust=False).mean()
ADXR = (ADX + ADX.shift(14)) / 2
```

#### (c) MACD 平滑 + ADX 自适应权重（核心创新点）

```
MACD(12, 26, 9)：
histogram = 2 * (DIF - DEA)                     # 注意乘 2，与 OKX 显示口径一致

hist_weight = min(0.7 + adx / 200, 1.0)         # ADX 越高历史权重越大（趋势中更平滑）
curr_weight = 1 - hist_weight

smoothed_histogram[i] = smoothed[i-1] * hist_weight + histogram[i] * curr_weight
（首根：smoothed = histogram）

hist_smooth_diff = histogram - smoothed_histogram
```

对应函数：`get_adaptive_smooth_weight(adx_value, base_weight=0.7)`。

#### (d) ATR

TR 的 `ewm(alpha=1/14, adjust=False).mean()`；详情页显示 `ATR` 与 `ATR% = ATR / close * 100`。

#### (e) 三种资金曲线（每笔平仓后按 profit_pct 更新，profit_pct 已含 10 倍杠杆）

```
fix_money      += 100 * profit_pct / 100                       # 定投：每笔固定 100 本金
contract_money *= (1 + profit_pct / 100)                       # 复投：复利

# 混合：本金随资金规模按 1.6 倍阶梯放大，下限 100
if mix_money >= 100:
    base = 100 * 1.6 ** floor(log(mix_money / 100, 1.6))
else:
    base = 100
mix_money += base * profit_pct / 100
```

初始值均为 100。BOLL 策略中 `excluded=True` 的交易，三条曲线**均不变**。

#### (f) 长周期方向状态机（justice_flag，dual 与 boll 共用）

区别于单周期的简单正负判断，**方向翻转需 MACD 主线（DIF）确认**，否则进入等待态：

```python
if justice_flag == "smoothed_histogram":
    if histogram - smoothed_histogram > 0:
        if macd.macd[-1] > 0:                     # 上一根 DIF 在零轴上方 → 直接确认
            modify_flag = "rise"
        else:                                     # 否则进入等待上涨确认态
            justice_flag = "histogram"; dealjustice_flag = "waitRise"
    else:
        if macd.macd[-1] < 0:
            modify_flag = "fall"
        else:
            justice_flag = "histogram"; dealjustice_flag = "waitFall"
elif justice_flag == "histogram":
    if dealjustice_flag == "waitRise":
        if histogram > 0:                         # 柱转正 → 确认上涨，回到正常态
            modify_flag = "rise"; justice_flag = "smoothed_histogram"
        else:
            modify_flag = "fall"                  # 保持下跌（未确认）
    elif dealjustice_flag == "waitFall":
        if histogram < 0:
            modify_flag = "fall"; justice_flag = "smoothed_histogram"
        else:
            modify_flag = "rise"
```

BOLL 核心中方向序列数值化：`rise=1.0 / fall=-1.0 / 无=0.0`（`_dir_to_value`），
通过自定义 feed `PandasDataWithLongDirection(bt.feeds.PandasData)`
（`lines = ('LONG_DIRECTION', 'LONG_CONF')`）注入 backtrader。

#### (g) 双周期对齐

```python
pd.merge_asof(df_short, df_long[['LONG_DIRECTION']],
              left_index=True, right_index=True, direction='backward')
```

即每根短周期 bar 取"其时间之前最近的一根长周期 bar"的方向；对齐后**截断**头部无长周期方向的数据。

---

### 1.1 单周期 Pro3（pro3_singletimeframe.py）

**定位**：只看单一周期，MACD 柱与其平滑线交叉即反手，永远在场。

- **信号**：每根 bar `modify_flag = "rise" if hist_smooth_diff > 0 else "fall"`（等于 0 时保持原值）。
- **配置**：`ENTRY_PRICE_TYPE='close'`、`EXIT_PRICE_TYPE='open'`、杠杆 `times=10.0`、`base_money=100`。
- **交易逻辑**（`_single_period_trade_logic`，在 backtrader `next()` 内）：
  1. 有持仓 + 信号翻转（signal_changed 且方向与持仓相反）→ 以当根 **open 价平仓**；
  2. 无持仓 + 信号改变 → 以当根 **close 价开仓**。
  实际效果是"平旧开新"的连续反手。
- **期末**：`stop()` 中若仍有持仓 → **强制平仓**（以最后一根 close 计入交易记录）。
- **盈亏**：多 `profit_pct = (exit/entry - 1) * 100 * 10`；空 `profit_pct = (1 - exit/entry) * 100 * 10`。
- **对外接口**：
  - `get_latest_data(instId, bar)` → `(latest_data: Series, last_trade_data: dict, atr: float)`
    latest_data 附带列：`DIF / MACD(柱) / ADX / ADXR / +DI / -DI / ADX_PREV / +DI_PREV / -DI_PREV / MODIFY_FLAG`（供主页卡片使用）；
  - `get_strategy_full_data(instId, bar, detail_mode=True)` → 详情页完整字典（见第 6 节结构）。

零点价格参考计算（使 histogram=0 的收盘价）：

```
histogram_zero = (prev_dea - prev_ema12*11/13 + prev_ema26*25/27) / (2/13 - 2/27)
```

### 1.2 双周期 Pro3（pro3_dualtimeframe.py）

**定位**：短周期简单交叉产生灵敏信号，长周期 justice_flag 状态机做方向过滤；开仓走**限价单**。

- **数据准备**（`_prepare_dual_data`）：短/长周期各取数 → 各自算 ADX →
  长周期 `_fill_long_direction_series` 用状态机生成 LONG_DIRECTION 序列 → merge_asof 对齐 → 截断。
- **短周期信号**：与单周期相同的简单交叉判断。
- **开仓（限价单机制）**：无持仓 + 短周期信号刚改变 + 信号方向与长周期方向**一致** →
  提交限价单，价格 = 当根 **open**，有效期 `limit_order_ttl = 4` 根 bar：
  - 后续 bar 若 `low <= order_price <= high` → **成交**（filled，成交价 = order_price）；
  - 挂单期间短周期信号反向 → **撤单**（cancelled）；
  - 4 根 bar 未成交 → **过期作废**（expired）。
- **平仓（市价，取当根 open 价）**，两条件满足其一即平：
  1. 短周期信号刚翻转且与长周期方向不一致；
  2. 长周期方向翻转到持仓的反方向（`long_dir_against`）。
- **期末**：`stop()` **不做**强制平仓（未平仓不产生记录）。
- **额外统计**：`entry_orders_submitted / entry_orders_filled / entry_orders_expired /
  entry_orders_cancelled`、`total_signal_flips`（短周期信号翻转总数）、
  `filtered_signals`（被长周期方向过滤掉的信号数）。
- **年化**：`(contract_money/100) ** (365/回测实际天数) - 1`（几何年化）。
- **接口**：
  - `get_latest_data_dual(instId, short_bar, long_bar)` →
    `(latest_data, last_trade_data, atr, long_direction, df_short, df_long)`；
  - `get_strategy_full_data_dual(instId, short_bar, long_bar)` → 详情字典，
    额外含 `dual_period: {short_bar, long_bar, long_direction}`。

### 1.3 BOLL 限价双周期（boll_limit_dualtimeframe.py + trend_strategy_boll_limit_scheduler_like.py）

**定位**：长周期状态机定方向，短周期布林带触边即入/触边即出（赚通道宽度），
长周期反转导致的亏损单**剔除**不计入统计。

核心策略 `DualTimeframeBollLimitStrategy(bt.Strategy)`：

- 参数：`boll_period=20, boll_dev=2.0, order_size=1.0, leverage_times=10.0`；
- 指标：`bt.indicators.BollingerBands(close, period=20, devfactor=2.0)`；
- **开仓**（空仓时）：
  - `long_dir == 'rise'` 且当根触碰下轨（`low <= boll_bot <= high`）→ 以**下轨价开多**；
  - `long_dir == 'fall'` 且当根触碰上轨（`low <= boll_top <= high`）→ 以**上轨价开空**；
- **平仓**：
  - 持多 + 触上轨 → 以**上轨价平多**；
  - 持空 + 触下轨 → 以**下轨价平空**；
- **剔除机制**（本策略灵魂）：持仓期间长周期方向发生反转（`position_flip=True`）
  **且**该笔 `profit < 0` → 标记 `excluded=True`：
  - 不计入胜率/盈亏/资金曲线等所有统计；
  - 前端交易记录整行置灰，状态列显示"剔除"；
- `max_win / max_loss` 由持仓期间 `max_price_since_entry / min_price_since_entry` 推算。

适配器 `get_boll_strategy_full_data(instId, short_bar="1H", long_bar="4H")` 流程：
关闭核心模块打印 → monkey-patch 取数函数 → 调用 `boll_module.get_latest_data` →
`_build_boll_detail_response_from_module` 组装响应 → `finally` 恢复。

响应差异：`market` 含 `is_boll: True` 和 `long_direction`，MACD 相关字段置 0；
`stats` 含 `excluded_trades`；`current_position` 固定显示"无持仓"。

### 1.4 三策略对比总表

| 维度 | 单周期 Pro3 | 双周期 Pro3 | BOLL 限价 |
|---|---|---|---|
| 方向来源 | 短周期 hist_smooth_diff 正负 | 短周期交叉 + 长周期状态机确认 | 长周期状态机定方向 |
| 入场 | 信号翻转，close 价市价 | 限价单（open 价，4 bar TTL，可撤/可过期） | 触布林轨即以轨道价入场 |
| 出场 | 反向信号，open 价 | 短周期反向或长周期反转，open 价市价 | 触对侧轨道即以轨道价出场 |
| 持仓模式 | 永远在场（连续反手） | 可空仓（信号被长周期过滤） | 可空仓 |
| 期末强平 | 有 | 无 | 无 |
| 特色 | 最简单、信号最多 | 限价单统计、信号过滤统计 | 亏损剔除机制（excluded） |
| 杠杆 | 10 倍 | 10 倍 | 10 倍 |

---

## 2. 周期选择器

### 2.1 详情页（detail.html）三组周期控件

按当前策略显隐切换（`togglePeriodSelectors()`）：

| 控件 | 结构 | 默认值 | 说明 |
|---|---|---|---|
| `#single-bar-group` | 11 个按钮：3m/5m/15m/30m/1H/2H/4H/6H/12H/1D/1W | 1H（带 active 类） | single 策略专用 |
| `#dual-short-bar` + `#dual-long-bar` | 下拉：短 5m/15m/1H；长 1H/4H/1D/1W | 短 1H / 长 4H | dual 策略专用 |
| `#boll-short-bar` + `#boll-long-bar` | 下拉，选项同上 | 短 15m / 长 4H | boll 策略专用 |

**交互逻辑**：

- 单周期按钮：点击 → 更新 `CURRENT_BAR` → 切换 active 类 → `loadDetail()`；
- 双周期/BOLL **短周期联动**：改短周期时按映射表自动重置长周期后再 `loadDetail()`：

```javascript
const DUAL_DEFAULT_MAP = { '5m': '1H', '15m': '4H', '1H': '1D' };
const BOLL_DEFAULT_MAP = { '5m': '1H', '15m': '4H', '1H': '1D' };
```

- 单独改长周期下拉也触发 `loadDetail()`。

JS 状态变量初始值：

```javascript
let CURRENT_BAR = '1H', CURRENT_STRATEGY = 'single';
let DUAL_SHORT_BAR = '1H', DUAL_LONG_BAR = '1D';
let BOLL_SHORT_BAR = '15m', BOLL_LONG_BAR = '4H';
```

### 2.2 后端默认长周期映射（strategy_util.py）

前端未传 `long_bar` 时自动映射：

```python
DEFAULT_LONG_BAR_MAP = {
    "3m": "15m", "5m": "30m", "15m": "1H", "30m": "4H",
    "1H": "4H", "2H": "1D", "4H": "1D",
    "6H": "1W", "12H": "1W", "1D": "1W", "1W": "1W",
}
# dual 兜底 "1D"，boll 兜底 "4H"
```

### 2.3 主页周期条（main.js `renderBarSelector`）

`#bar-selector` 由 JS 动态生成：`AVAILABLE_BARS = ['3m','5m','15m','30m','1H','2H','4H','6H','12H','1D','1W']`，
默认 `CURRENT_BAR='1H'`。按钮类名 `m-btn m-btn-primary m-btn-xs m-btn-outline m-btn-pill [active]`。
切换后：清空 `coinDataCache` → `startRefresh()` 全量重新加载。

---

## 3. 币种选择器

### 3.1 配置持久化（config.json，由 real_strategy_adapter.py 管理）

```json
{
    "all_coins": ["BTC-USDT-SWAP", ...],      // 固定币种（市值大，不随意删改）
    "floating_coins": [...],                   // 浮动币种（趋势扫描页临时添加，可增删）
    "default_selected": [...],                 // 当前选中（主页监控卡片）
    "starred_coins": [...]                     // 星标币种
}
```

关键函数：`get_all_coins()`（固定 + 浮动去重，浮动排最后）、`get_fixed_coins`、
`get_floating_coins`、`add_floating_coins`（去重追加）、`remove_floating_coin`
（同时从 default_selected 移除）、`get/set_selected_coins`、`get/set/clear_starred_coins`、
`clear_all_selections`、`sync_coin_list`（从 crypto_coins.csv 的 inst_id 列合并进 all_coins，
保持 CSV 顺序 + 保留手动添加项）。

### 3.2 主页选择器（main.js `renderCoinSelector`）

每个币种渲染为一个 `.coin-tag`（div），组成部分：

1. **星标图标** `span.star-icon`：`★`（已星标，加 starred 类，tag 加 star-highlight 类）/ `☆`；
   点击时 `e.stopPropagation()` 后 `toggleStar(coin)` → 更新所有图标（`updateAllStarIcons`，
   不重建选择器）→ `POST /api/strategy/starred` 持久化；
2. **名称** `coin.replace('-USDT-SWAP', '')`；
3. **浮动币种**额外：`coin-floating` 类 + `×` 删除按钮（MDialog.confirm 确认后
   `POST /api/strategy/floating-coins/remove`，成功后刷新列表与卡片）。

**选中逻辑**：点击 tag 仅前端 `classList.toggle('active')`；点"保存选择"按钮才收集
所有 `.coin-tag.active` 的 `dataset.coin` → `POST /api/strategy/config {selected_coins:[...]}`
（后端校验每个必须 ∈ all_coins）→ 成功后 `SELECTED_COINS = selected`、清缓存、重建卡片。

**操作按钮**（7 个）：筛选（展开高级筛选面板）、🔄同步（POST /api/strategy/sync-coins）、
全选所有 / 重置选择（纯前端加/去 active）、全选星标（仅星标币加 active）、
重置星标（POST /api/strategy/starred/clear）、保存选择。

### 3.3 详情页选择器（detail.html `#detail-symbol-select`）

`<select>` 下拉，两个 `<optgroup label="固定币种"/"浮动币种">`，由 Flask 模板注入。
`change` 事件：更新 SYMBOL → `history.replaceState(null, '', '/detail/新币种?bar=...')`
（不刷新页面）→ 更新页面标题 → `loadDetail()`。

---

## 4. 策略选择按钮的切换逻辑（detail.html）

三个按钮，`data-strategy="single" | "dual" | "boll"`，显示名："单周期Pro3 / 双周期Pro3 / BOLL限价"。

```javascript
strategyBtns.forEach(btn => btn.addEventListener('click', () => {
    if (btn.dataset.strategy === CURRENT_STRATEGY) return;
    CURRENT_STRATEGY = btn.dataset.strategy;
    strategyBtns.forEach(b => b.classList.remove('active'));
    btn.classList.add('active');
    togglePeriodSelectors();   // single→按钮组；dual→dual下拉；boll→boll下拉
    loadDetail();
}));
```

`loadDetail()` 按策略拼请求 URL：

| 策略 | URL |
|---|---|
| single | `/api/strategy/detail/SYM?bar=${CURRENT_BAR}&strategy=single` |
| dual | `/api/strategy/detail/SYM?bar=${DUAL_SHORT_BAR}&strategy=dual&long_bar=${DUAL_LONG_BAR}` |
| boll | `/api/strategy/detail/SYM?bar=${BOLL_SHORT_BAR}&strategy=boll&long_bar=${BOLL_LONG_BAR}` |

后端分发（strategy_util.py）要点：

- single 分支：`del sys.modules['pro3_singletimeframe']` 强制重载 → 置零打印开关 →
  `get_strategy_full_data(instId, bar, detail_mode=True)`；
- dual 分支：同时删除 `pro3_dualtimeframe` 与 `pro3_singletimeframe` 缓存后重载 →
  `get_strategy_full_data_dual(instId, short_bar, long_bar)`；
- boll 分支：直接 `from boll_limit_dualtimeframe import get_boll_strategy_full_data` 调用。

---

## 5. 数据展示面板的渲染机制

### 5.1 详情页三大面板（loadDetail 成功回调依次执行）

**价格精度函数**（全站统一规则）：

```javascript
function fmtPrice(v) {
    if (v < 10)   return v.toFixed(3);
    if (v < 100)  return v.toFixed(2);
    if (v < 1000) return v.toFixed(1);
    return Math.round(v).toString();
}
```

#### (1) renderMarketSummary(market, position) → `#market-summary`

- **BOLL 模式**（`market.is_boll === true`）显示：最新价格、长周期方向、ADX、ATR/ATR%、
  当前持仓、持仓盈亏、入场价；
- **Pro3 模式**显示：最新价格、趋势方向（rise→"上涨↑" up 色 / fall→"下跌↓" down 色）、
  MACD 柱、DIF、ADX（>25 高亮）、+DI/-DI、平滑MACD、当前权重（hist_weight/curr_weight）、
  ATR，以及持仓信息（当前持仓/持仓盈亏/入场价）。

#### (2) renderTradeRecords(records) → `#trade-tbody`

12 列：`# | 方向 | 开仓时间 | 开仓价 | 平仓时间 | 平仓价 | 盈亏% | 最大盈利% | 最大亏损% | 持仓时长 | ADX | 状态`。

- 盈亏正数红色（up 类）、负数绿色（down 类），按中国习惯红涨绿跌；
- BOLL 的 `excluded` 行：整行置灰样式，状态列显示"剔除"；正常行状态为"已平仓"。

#### (3) renderStats(stats) → `#stats-grid`

6 张卡片（详见第 6 节字段）；BOLL 模式隐藏"ADX分析"卡，"资金明细"卡多一行"剔除交易数"。

同时更新副标题：`策略显示名 · ${backtest_days}天回测 · ${total_trades}笔交易`。

### 5.2 主页卡片（main.js）

- `initCards()`：为每个 SELECTED_COINS 生成占位卡片（骨架）；
- `startRefresh()`：**串行**遍历币种，逐个
  `fetch('/api/strategy/coin/' + coin + '?bar=' + CURRENT_BAR)`，
  相邻请求间隔 **300ms**（防止后端并发压力）；成功写 `coinDataCache[coin]` 并渲染；
  失败时用缓存兜底渲染（卡片标记"缓存"状态）；
- 卡片 5 行布局：
  1. 最新价格；
  2. MACD 柱 | DIF；
  3. 市场状态（12 态之一）| 趋势方向（上涨/下跌/观望）；
  4. 最后交易价 | 当前盈亏（正红负绿）；
  5. 交易时间 | 持仓时长；
  底部两个链接："查看详情→"（`/detail/币种?bar=当前周期`）、"多周期总览→"（`/multi-period/币种`）；
- 全部加载完成后 `updateOverallMarket()`：
  `up > total*0.6` → "大盘整体向上"；`down > total*0.6` → "大盘整体向下"；否则"大盘走势分化 / 震荡"，
  均附 `(多头: N, 空头: N, 观望: N)`；
- **卡片拖拽排序**：HTML5 drag API（dragstart/dragover/drop），交换位置后把新顺序
  `POST /api/strategy/config` 持久化；
- **自动刷新**：`setInterval(startRefresh, N分钟)`，输入框配置，默认 1 分钟，0=关闭，上限 60。

### 5.3 颜色规则汇总

| 元素 | 规则 |
|---|---|
| 盈亏/趋势 | 正/上涨 → 红（up 类）；负/下跌 → 绿（down 类） |
| ADX（卡片） | >25 绿色，否则黄色 |
| ADX（批量表格） | ≥30 strong，≥20 moderate，否则 weak |
| MACD | 正绿负红（批量表格中） |
| SAR | 低于价格绿色（多头），否则红色 |

---

## 6. 统计指标的计算方法与显示内容

### 6.1 详情接口完整响应结构

```
{
  "market": {
      "timestamp", "price", "modify_flag",
      "macd_histogram", "dif", "smoothed_macd",
      "adx", "adxr", "plus_di", "minus_di",
      "hist_weight", "curr_weight",
      "atr", "atr_pct",
      // BOLL 专属：
      "is_boll": true, "long_direction": "rise"/"fall"
  },
  "current_position": {
      "direction": "多"/"空"/"无持仓",
      "entry_price", "entry_time", "current_profit_pct", "hold_duration"
  },
  "trade_records": [ {
      "index", "direction"("多"/"空"), "entry_time", "entry_price",
      "exit_time", "exit_price", "profit_pct",
      "max_win_pct", "max_loss_pct", "duration_h",
      "adx"(开仓时), "status"("已平仓"/"剔除"), "excluded"(bool, BOLL)
  } ],
  "stats": { ...见 6.2... },
  "backtest_days": N,
  // dual 专属：
  "dual_period": { "short_bar", "long_bar", "long_direction" }
}
```

### 6.2 stats 字段计算公式（`_build_stats_dict`，仅统计未剔除的已平仓交易）

| 字段 | 计算方法 |
|---|---|
| total_trades | 已平仓笔数 |
| winning_trades / losing_trades | profit>0 / profit≤0 笔数 |
| win_rate | winning / total * 100 |
| avg_profit_pct / avg_loss_pct | 盈利笔均值 / 亏损笔均值 |
| total_profit_pct / total_loss_pct | 盈利之和 / 亏损之和 |
| total_return_pct | 全部 profit_pct 之和 |
| profit_factor | total_profit / \|total_loss\|（无亏损时 inf） |
| profit_loss_ratio | avg_profit / \|avg_loss\| |
| max_single_profit_pct / max_single_loss_pct | 单笔最大盈利 / 最大亏损 |
| max_drawdown_pct | 基于**复投曲线**序列：max((peak - v) / peak) * 100 |
| calmar_ratio | 年化收益 / 最大回撤。单周期年化 = (contract/100)^(252/交易笔数) - 1；双周期 = (contract/100)^(365/回测天数) - 1 |
| contract_return_pct | (contract_money/100 - 1) * 100 |
| fix_money / contract_money / mix_money | 三资金曲线终值（初始 100，公式见 1.0(e)） |
| avg_total_dur_h / avg_win_dur_h / avg_loss_dur_h | 平均持仓小时（全部/盈利笔/亏损笔） |
| avg_retracement_pct | mean(max_win_pct - profit_pct)，利润回吐幅度 |
| adx_mean / adx_win_mean / adx_loss_mean | 开仓时 ADX 均值（全部/胜/负） |
| adx_corr | np.corrcoef(开仓ADX序列, profit序列)[0][1] |
| high_adx_win_rate / low_adx_win_rate | 开仓 ADX≥25 组与 <25 组各自胜率 |
| avg_hist_weight / avg_curr_weight | 全程平均自适应权重 |
| （dual）entry_orders_submitted/filled/expired/cancelled | 限价单四态计数 |
| （dual）total_signal_flips / filtered_signals | 信号翻转总数 / 被长周期过滤数 |
| （boll）excluded_trades | 剔除笔数 |

### 6.3 前端 6 张统计卡片分组

1. **收益概况**：总收益%、复投收益%、定投资金、复投资金、混合资金；
2. **交易统计**：总笔数、胜率、盈利/亏损笔数、平均盈利%、平均亏损%、盈亏比；
3. **风险指标**：最大回撤%、Calmar、盈利因子、单笔最大盈利/亏损%、平均回吐%；
4. **持仓与ADX**：平均持仓时长（全部/盈利/亏损）、ADX 均值；
5. **ADX分析**（BOLL 隐藏）：盈利/亏损 ADX 均值、ADX-盈亏相关性、高/低 ADX 胜率；
6. **资金明细**：三资金曲线 + 平均权重（BOLL 多"剔除交易数"）。

### 6.4 主页卡片指标（real_strategy_adapter.calculate_single_coin_data）

**当前盈亏**（10 倍杠杆浮盈）：

```python
if modify_flag == 'rise':
    current_profit = (price / last_trade_price - 1) * 100 * 10.0
elif modify_flag == 'fall':
    current_profit = (1 - price / last_trade_price) * 100 * 10.0
```

**持仓时长**：`now - trade_time` 差值格式化为 `N天N小时N分` 或 `N小时N分`；
交易时间格式化为 `%Y-%#m-%#d %H:%M:%S`（Windows 去前导零写法）。

**12 种市场状态**（`determine_market_state(adx, adxr, adx_prev, +di, -di, +di_prev, -di_prev)`）：

```
adx_trend: ADX变化 > 0.5 → rising；< -0.5 → falling；否则 flat
gap_expanding: |+DI - -DI| > |prev差| + 0.5
di_cross_up/down: DI 上/下穿检测

判定顺序：
1. ADX < 20                          → 震荡无趋势
2. ADX > 50 且 falling               → 趋势过热预警
3. adx_prev > 25 且 adx < 25         → 强上涨转震荡 / 强下跌转震荡（按DI大小）
4. 20≤ADX≤25 且 rising 且 DI交叉     → 震荡转上涨初期 / 震荡转下跌初期
5. ADX > 25：
   +DI > -DI:  扩张且rising→上涨趋势增强；flat→上涨趋势稳定；否则→上涨趋势减弱
   否则:       扩张且rising→下跌趋势增强；flat→下跌趋势稳定；否则→下跌趋势减弱
6. 兜底：按 DI 大小给"上涨/下跌趋势稳定"
```

**IO 容错**：错误消息含 `closed file/10035/connection/timeout/ssl/i/o` 之一时，
重试（默认 2 次），等待 `0.5*(attempt+1)` 秒。

---

## 7. 前端 JS 与后端 API 交互契约

### 7.1 接口总表

| 接口 | 方法 | 参数 | 返回 data |
|---|---|---|---|
| `/api/strategy/config` | GET | — | `{all_coins, fixed_coins, floating_coins, selected_coins, starred_coins}` |
| `/api/strategy/config` | POST | `{selected_coins:[]}` | 保存选中（校验 ∈ all_coins，否则 400） |
| `/api/strategy/coin/<symbol>` | GET | `bar` | 主页卡片 JSON（见 7.2） |
| `/api/strategy/detail/<symbol>` | GET | `bar, strategy, long_bar` | 详情完整字典（见 6.1） |
| `/api/strategy/starred` | GET / POST | `{starred_coins:[]}` | 星标读写 |
| `/api/strategy/starred/clear` | POST | — | 清空星标 |
| `/api/strategy/clear-selections` | POST | — | 清空选中 |
| `/api/strategy/sync-coins` | POST | — | CSV 同步后的完整配置 |
| `/api/strategy/floating-coins/add` | POST | `{coins:[]}` | 更新后的列表 + 新增数 |
| `/api/strategy/floating-coins/remove` | POST | `{coin}` | 更新后的列表 |
| `/api/batch/update` | POST | — | 启动批量多周期趋势分析 |
| `/api/batch/progress` | GET | — | 进度（前端 1.5 秒轮询） |
| `/api/batch/csv-data` / `csv-filtered` | GET | — | 31 列完整表 / 一致性筛选表 |

统一响应：`{code: 200/400/500, message, data}`。
detail 接口异常时，若错误属网络类（Timeout/Connect/Protocol/SSL/Network/Read 关键字），
返回友好中文提示"网络连接不稳定，请稍后重试"类文案。

### 7.2 主页卡片 JSON（/api/strategy/coin/<symbol> 的 data）

```json
{
    "timestamp": "2026-07-27 10:00:00",
    "symbol": "BTC-USDT-SWAP",
    "price": 12345.6789,
    "indicators": {
        "macd": { "histogram": 1.2345, "dif": 0.5678 },
        "adx":  { "adx": 28.1, "adxr": 25.0, "+di": 30.2, "-di": 15.1 },
        "atr": 123.45
    },
    "analysis": {
        "market_state": "上涨趋势增强",
        "action_signal": "上涨",
        "modify_flag": "rise"
    },
    "trade_info": {
        "last_trade_price": 12000.0,
        "current_profit": 28.8,
        "trade_time": "2026-7-25 18:18:16",
        "hold_time": "1天15小时42分"
    }
}
```

### 7.3 交互模式

- 全部原生 `fetch` + JSON，无任何前端框架；
- 错误提示统一 `MDialog.alert({message, type})`，确认操作用 `MDialog.confirm`；
- 主页串行加载（300ms 间隔）+ 缓存兜底 + 自动刷新定时器；
- 详情页每次切换（币种/策略/周期）都是一次全新的 detail 请求（后端实时回测，无缓存）。

---

## 8. 两页面完整功能流程

### 8.1 index.html（主监控台）

**页面结构**：币种选择面板（tag 列表 + 7 个操作按钮）→ 高级筛选面板（可折叠）→
周期条 `#bar-selector` → 综合行情 `#overall-market` → 卡片区 `#data-panel` →
手动刷新按钮 + 自动刷新配置 → 批量多周期趋势分析面板。
引入 `mdialog.js` 与 `main.js`。

**启动流程**：

```
DOMContentLoaded → init()
  → GET /api/strategy/config（失败用默认 BTC/ETH/NEAR）
  → renderCoinSelector() → renderBarSelector() → initCards() → startRefresh()
```

**运行时交互**：币种勾选/保存、星标切换、周期切换（清缓存重载）、卡片拖拽排序（持久化顺序）、
手动/自动刷新、筛选面板（趋势 1H/4H/1D ↑↓、组合 4H+1D 双涨双跌、盈亏正负、
ADX/ATR 阈值输入、排序下拉、批量星标）。

**批量趋势分析流程**：

```
点击"开始分析" → POST /api/batch/update
  → setInterval 1.5s 轮询 GET /api/batch/progress（更新进度条与统计条）
  → 完成后 loadTableData()：GET /api/batch/csv-data 或 csv-filtered
  → 渲染 31 列表格：排名/币种/交易对/名称
      + [趋势/交易价/盈亏%/MACD/DIF/ADX/ATR%/SAR] × 1H/4H/1D 三周期
      + 3 个时间列
  → 支持：表头点击排序、周期趋势筛选、MACD/ADX 范围筛选、
      勾选批量星标（二级弹窗选"重置并添加"或"增量添加"）、全屏、
      简洁视图（隐藏 .indicator-col）
```

### 8.2 detail.html（详情页）

**页面结构**：返回链接 + 标题 → 币种下拉（optgroup 固定/浮动）→ 策略按钮组 →
三组周期选择器（按策略显隐）→ 行情概览 `#market-summary` →
交易记录表 `#trade-tbody`（12 列）→ 回测统计 `#stats-grid`（6 卡片）。JS 全部内联。

**完整流程**：

```
进入 /detail/<symbol>?bar=1H
  → 初始化状态变量（CURRENT_STRATEGY='single', CURRENT_BAR=URL的bar或'1H'）
  → loadDetail()
      → GET /api/strategy/detail/SYM?bar=..&strategy=..[&long_bar=..]
      → 后端：strategy_util 分发 → del sys.modules 重载策略模块
             → OKX 拉约 500 根 K 线 → ADX/MACD平滑等指标
             → backtrader 回测 → 组装 market/position/records/stats
      → 前端：更新副标题（策略名 · N天回测 · N笔交易）
             → renderMarketSummary() → renderTradeRecords() → renderStats()

任何切换（币种下拉 / 策略按钮 / 周期按钮或下拉）
  → 只更新对应状态变量（币种切换额外 history.replaceState 同步 URL）
  → 重新 loadDetail()
```

---

## 9. 复刻清单

### 9.1 最小文件集

| 文件 | 作用 |
|---|---|
| `crypto/strategy/pro3_singletimeframe.py` | 单周期策略 + 全部共享基础设施（OKX 取数/ADX/自适应权重/响应构建工具） |
| `crypto/strategy/pro3_dualtimeframe.py` | 双周期策略（justice_flag 状态机 + 限价单机制） |
| `crypto/strategy/boll_limit_dualtimeframe.py` | BOLL 适配器（monkey-patch 取数 + 响应构建） |
| `crypto/trend_strategy_boll_limit_scheduler_like.py` | BOLL backtrader 核心（触边交易 + 剔除机制） |
| `crypto/strategy_util.py` | 策略分发器 + DEFAULT_LONG_BAR_MAP |
| `crypto/real_strategy_adapter.py` | 主页卡片数据 + config.json 币种管理 + 12 态市场状态 |
| `crypto/app.py`（路由部分） | Flask 页面与 API 路由 |
| `crypto/templates/index.html` + `static/js/main.js` + `mdialog.js` + CSS | 主页 |
| `crypto/templates/detail.html`（内联 JS） | 详情页 |
| `crypto/config.json`、`crypto/crypto_coins.csv` | 币种配置与 CSV 源 |
| `crypto/api_config.py` | OKX API 密钥配置 |

### 9.2 依赖

```
Flask、backtrader、pandas、numpy、httpx、python-okx（OKX SDK）
```

### 9.3 复刻要点核对表

- [ ] histogram 必须 `2*(DIF-DEA)`（乘 2）
- [ ] 自适应权重 `min(0.7 + adx/200, 1.0)`，首根平滑值 = histogram 本身
- [ ] ADX 全程 Wilder ewm(alpha=1/14, adjust=False)
- [ ] 时间戳 +8 小时；每次新建 MarketAPI；4 次重试退避
- [ ] 单周期：close 开 / open 平 / 期末强平；双周期与 BOLL 无期末强平
- [ ] 双周期限价单：open 价、TTL 4 bar、low≤price≤high 成交、反向撤单
- [ ] justice_flag 状态机的等待态与 macd[-1] 零轴确认逻辑逐行照抄
- [ ] BOLL：BollingerBands(20, 2.0)、触边判定 `low <= 轨道 <= high`、以轨道价成交
- [ ] BOLL 剔除条件：position_flip 且 profit<0，剔除单不进任何统计与资金曲线
- [ ] 盈亏一律 ×10 杠杆；三资金曲线公式（含 1.6 阶梯混合曲线）
- [ ] merge_asof direction='backward' 对齐 + 头部截断
- [ ] 价格精度四档规则；红涨绿跌配色；ADX>25 高亮
- [ ] 主页串行加载 300ms 间隔 + coinDataCache 兜底
- [ ] 双周期/BOLL 短周期联动映射 {'5m':'1H','15m':'4H','1H':'1D'}
- [ ] strategy_util 每次 del sys.modules 强制重载（保证代码热更新生效）
